using System;

namespace mehods
{
    class Program
    {
        static void Main(string[] args)
        {
            #region

            //string str = "Xacmaz";
            //string reverse = "";
            //int length = str.Length - 1;

            //while (length >= 0)
            //{
            //    reverse += str[length];
            //    length--;
            //}

            //Console.WriteLine(reverse);


            //int[] arr = { 11, -17, 30, -25, 44, -32, -98 };
            //int loop = 0;

            //for (loop = 0; loop < arr.Length; loop++)
            //{
            //    if (arr[loop] < 0)
            //        arr[loop] = -arr[loop];
            //}

            //Console.WriteLine("Array elements:");
            //for (loop = 0; loop < arr.Length; loop++)
            //{
            //    Console.Write(arr[loop] + " ");
                #endregion
            }
        }
    }
}